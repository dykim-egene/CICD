pipeline {
    agent any
    
    stages {
        
        stage('github-clone') {
            steps {
                git branch: 'feature_05261', credentialsId: 'github_key', url: '{REPOSITORY URL}'
            }
        }
        
   		// stage...
   	}
}
